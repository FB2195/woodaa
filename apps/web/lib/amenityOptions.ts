// Re-exported from @woodaa/validators, which is the canonical source now
// that the search results toolbar also needs this list server-side (for
// facet counts) - kept as a thin re-export here so existing imports
// throughout apps/web don't need to change.
export { AMENITY_OPTIONS } from "@woodaa/validators";
